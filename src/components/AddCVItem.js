import React, { Component } from "react";
import { connect } from "react-redux";

class AddCVItem extends Component {

    render() {
        return (
            <div>
            </div>
        )
    }
}

export default connect()(AddCVItem);